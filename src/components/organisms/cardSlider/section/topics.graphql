query getSectionTopics(
	$language: Language!
	$first: Int! = 6
	$after: String = null
) {
	topics(
		language: $language
		first: $first
		after: $after
		orderBy: { field: ID, direction: DESC }
	) {
		nodes {
			...cardTopic
		}
		pageInfo {
			hasNextPage
			endCursor
		}
	}
}
