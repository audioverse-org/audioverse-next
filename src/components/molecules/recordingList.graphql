fragment recordingList on Recording {
	id
	title
	canonicalPath(useFuturePath: true)
	description
	duration
	imageWithFallback {
		url(size: 50)
	}
	persons {
		...personLockup
	}
	hasVideo
	recordingDate
}
