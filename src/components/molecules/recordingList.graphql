fragment recordingList on Recording {
	id
	title
	canonicalPath(useFuturePath: true)
	description
	duration
	imageWithFallback {
		url(size: 50)
	}
	persons(role: SPEAKER) {
		...personLockup
	}
	hasVideo
	recordingDate
}
