fragment cardWithPlayable on Recording {
	...teaseRecording
	sequence {
		canonicalPath(useFuturePath: true)
		image {
			url(size: 1000)
		}
	}
	sponsor {
		...sponsorLockup
	}
	writers: persons(role: WRITER) {
		...personLockup
	}
}
