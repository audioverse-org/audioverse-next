fragment cardSponsor on Sponsor {
	__typename
	id
	title
	canonicalPath(useFuturePath: true)
	image {
		url(size: 128)
	}
	collections {
		aggregate {
			count
		}
	}
	sequences {
		aggregate {
			count
		}
	}
	recordings {
		aggregate {
			count
		}
	}
}
