fragment cardTopic on Topic {
	id
	title
	summary
	duration
	items {
		nodes {
			entity {
				__typename
			}
		}
		aggregate {
			count
		}
	}
	# TODO: add canonical path to API
	canonicalPath: title
}
