fragment bibleBookDetailVersion on Collection {
	id
	description
	sponsor {
		title
		website
	}
}

fragment bibleBookDetailBook on Sequence {
	id
	title
}

fragment bibleBookDetailChapterPartial on Recording {
	id
	title
	...andMiniplayer
	...teaseRecording
}

fragment bibleBookDetailChapterFull on Recording {
	title
	transcript {
		text
	}
	...sequenceNav
	...player
}

query getAudiobibleBookPathsData($language: Language!, $first: Int) {
	recordings(language: $language, first: $first, contentType: BIBLE_CHAPTER) {
		nodes {
			canonicalPath(useFuturePath: true)
		}
		pageInfo {
			hasNextPage
		}
	}
}
