query getVersionDetailPageData($id: ID!) {
	audiobible(id: $id) {
		title
		copyrightText
		sponsor {
			name
			url
		}
		books {
			...cardBibleBook
		}
	}
}

query getVersionDetailPathData {
	audiobibles {
		nodes {
			id
		}
	}
}
