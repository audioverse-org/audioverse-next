# TODO: Consider if featured collection query needs to account for
# different languages
query getDiscoverCollectionsPageData($collectionId: ID = null) {
	websiteFeaturedCollection {
		...cardFavoriteEntity
	}
}
