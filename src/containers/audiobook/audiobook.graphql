query getAudiobookDetailPageData($id: ID!) {
    audiobook(id: $id) {
        recordings {
            nodes {
                id
                title
                audioFiles {
                    url
                }
            }
        }
    }
}

query getAudiobookDetailPathsData(
    $language: Language!,
    $first: Int,
    $offset: Int
) {
    audiobooks(
        language: $language,
        first: $first,
        offset: $offset
    ) {
        nodes {
            id
        }
    }
}
