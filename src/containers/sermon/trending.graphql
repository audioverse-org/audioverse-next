query getTrendingTeachingsPageData($language: Language!, $hasVideo: Boolean) {
	recordings: popularRecordings(
		language: $language
		first: 24
		hasVideo: $hasVideo
	) {
		nodes {
			recording {
				...cardRecording
			}
		}
	}
}
