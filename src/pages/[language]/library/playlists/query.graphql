query getLibraryPlaylists($language: Language!) {
	me {
		user {
			playlists(
				language: $language
				first: 1500
				orderBy: [{ field: CREATED_AT, direction: DESC }]
			) {
				edges {
					node {
						...PlaylistCard
					}
				}
			}
		}
	}
}
