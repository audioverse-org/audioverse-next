query getGraphqlVersionIndex($collectionId: ID!) {
	collection(id: $collectionId) {
		id
		title
		sequences(first: 66) {
			nodes {
				id
				title
				recordings(first: 150) {
					nodes {
						id
						title
					}
				}
			}
		}
	}
}